
function concatStr(str1, str2) {

	// Écrivez votre code ici

    return str1 + " " + "| " + str2

    // return `${str1} | ${str2}`;
}

// Afficher la sortie

console.log(concatStr("True", "False"));
console.log(concatStr("On", "Off"));
console.log(concatStr("A", "B"));

// Exercice 3:

// Concaténer deux chaînes en une seule chaîne Avec deux chaînes, str1 et str2, renvoyez une seule chaîne au format « str1 | str2 ».

// Exemple:

// concatStr("True", "False") ➞ "True | False"

// concatStr("On", "Off") ➞ "On | Off"

// concatStr("A", "B") ➞ "A | B"