
    function sum(tab) {

        // Écrivez votre code ici

       let summ = 0;
       
       for (const a of tab)
        summ += a;
        return summ 
        
       }


 

// Afficher la sortie

console.log(sum([1, 1, 1]));
console.log(sum([1, 2, 3]));
console.log(sum([-2, 2, 1]));

// Exercice 7:

// Créez une fonction qui prend un tableau et renvoie la somme de tous les nombres du tableau.


// Exemple:

// sum([1, 1, 1]) ➞ 3

// sum([1, 2, 3]) ➞ 6

// sum([-2, 2, 1]) ➞ 1

/********************************************************************************************** */

// const array = [tab];
        // let sum = 0;

        // for (let i = 0; i < array.length; i++) {
        // return sum += tab[i];

        // const broj = [tab]

        // return broj.reduce(tab)

        // // return [tab].reduce((a, b, c)=> a + b + c);
        
        // let array1 = [tab];

        // // 0 + 1 + 2 + 3 + 4
        // return tab.reduce(
        //   (a, b , c) => a + b + c
        // );


         // let sequence = [tab];
        // for (var i = 0; i < tab.length; i++) {
        // return (tab[i]);
        // }